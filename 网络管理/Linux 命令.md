
# # 🧩 Linux 一天速成命令总结（网络工程师面试版）

---

# ## Ⅰ. 文件与目录操作（File & Directory）

### **`ls` — 列出文件**

```
ls
ls -l      # 显示详细信息
ls -a      # 显示隐藏文件
ls -lh     # 人类可读大小
```

### **`cd` — 切换目录**

```
cd /path/
cd ..      # 上一级
cd ~       # 回到家目录
```

### **`pwd` — 显示当前路径**

```
pwd
```

### **`mkdir` — 创建目录**

```
mkdir folder
mkdir -p a/b/c    # 递归创建
```

### **`cp` — 拷贝**

```
cp a.txt b.txt
cp -r dir1 dir2   # 拷贝目录
```

### **`mv` — 移动/重命名**

```
mv a.txt folder/
mv old new
```

### **`rm` — 删除**

```
rm file
rm -r folder
rm -rf folder   # 强制删除（危险）
```

---

# ## Ⅱ. 权限与所有者（Permissions & Ownership）

### 权限数字含义

```
r = 4 （read）
w = 2 （write）
x = 1 （execute）
```

### **`chmod` — 修改权限**

```
chmod 755 file        # rwx r-x r-x
chmod 644 file        # rw- r-- r--
chmod u+x file        # user 加执行权限
chmod g-w file        # group 去写权限
```

### **`chown` — 修改文件拥有者/用户组**

```
chown user file           # 只改拥有者
chown user:group file     # 改拥有者 + 用户组
chown root:root file      # 属于 root
```

---

# ## Ⅲ. 查找与内容搜索（Find & Search）

### **`find` — 按文件查找**

```
find . -name "*.txt"     # 在当前目录查找
find / -size +10M        # 查全系统大于 10M 的文件
find . -type d           # 查目录
find . -type f           # 查文件
```

### 路径说明：

- `.` = 当前目录
    
- `/` = 系统根目录（搜索范围最大）
    

---

### **`grep` — 搜索文本内容**

```
grep "hello" file.txt
grep -n "error" log.txt     # 显示行号
grep -i "keyword" file      # 忽略大小写
grep -r "TODO" .            # 递归搜索
```

---

# ## Ⅳ. 文本查看与处理（Viewing & Processing）

### **`cat` — 查看文件内容**

```
cat file
```

### **`less` — 分页查看（看大文件）**

```
less bigfile.log
```

### **`head` / `tail` — 看前/后几行**

```
head -n 20 file
tail -n 50 file
tail -f log.txt    # 实时日志
```

---

# ## Ⅴ. 文本处理工具：sed / awk

---

## ### 🟥 sed（流式文本处理）

### **替换（不修改原文件）**

```
sed 's/error/warning/g' file
```

### **直接修改文件**

```
sed -i 's/error/warning/g' file
```

### **删除行**

```
sed '2d' file            # 删第2行
sed '1,3d' file          # 删1~3行
sed '/error/d' file      # 删含 error 的行
```

### **只打印符合条件的行**

```
sed -n '/error/p' file
```

---

## ### 🟦 awk（按列处理）

### **打印第1列**

```
awk '{print $1}' file
```

### **指定分隔符**

```
awk -F ',' '{print $2}' data.csv
```

### **条件过滤**

```
awk '$3 == "error" {print $1}' log.txt
```

### **打印最后一列**

```
awk '{print $NF}' file
```

### **统计行数**

```
awk 'END {print NR}' file
```

---

# ## Ⅵ. 进程管理（ps / top / kill / systemctl）

---

## ### 🟦 `ps aux` 字段解释

```
USER PID %CPU %MEM VSZ RSS STAT START TIME COMMAND
```

### 常见字段含义：

|字段|意思|
|---|---|
|**PID**|进程 ID|
|**%CPU**|CPU 占比|
|**%MEM**|内存占比|
|**VSZ**|虚拟内存大小|
|**RSS**|实际占用内存|
|**STAT**|进程状态|
|**TIME**|累计CPU时间|

---

## ### 🟥 STAT 状态符号（面试常考）

|状态符号|含义|
|---|---|
|**R**|Running，运行中|
|**S**|Sleep，睡眠|
|**D**|不可中断睡眠（I/O 等待）|
|**T**|停止|
|**Z**|Zombie，僵尸进程|
|**<**|高优先级|
|**N**|低优先级|
|**+**|前台进程|

---

## ### 🟦 `top` 常见字段

|字段|意思|
|---|---|
|**PR**|优先级|
|**NI**|nice 值|
|**VIRT**|虚拟内存|
|**RES**|常驻内存|
|**SHR**|共享内存|
|**%CPU**|CPU 占用|
|**%MEM**|内存占用|

---

## ### 🟥 kill 信号解释

```
kill -9 PID
kill -15 PID
kill -1 PID
```

|信号|含义|
|---|---|
|**-15 (TERM)**|优雅停止（推荐）|
|**-9 (KILL)**|强制终止（不能阻止）|
|**-1 (HUP)**|重启进程|

---

## ### 🟦 systemctl（管理服务）

```
systemctl start nginx
systemctl stop nginx
systemctl restart nginx
systemctl status nginx
```

---

# ## Ⅶ. 网络命令与符号解释（Networking）

---

## ### 🟥 ss -tulpn（核心重点）

```
ss -tulpn
```

|参数|意思|
|---|---|
|**-t**|TCP|
|**-u**|UDP|
|**-l**|只看监听状态 LISTEN|
|**-p**|显示进程名称|
|**-n**|数字显示，不解析域名|

### 用途：

> 查看系统哪些进程在监听哪些端口（面试必背）

---

## ### 🟦 ss 输出字段

```
Netid State Recv-Q Send-Q Local Address:Port Peer Address:Port Process
```

|字段|含义|
|---|---|
|**Netid**|tcp / udp|
|**State**|LISTEN / ESTABLISHED|
|**Local Address:Port**|本地监听地址和端口|
|**Peer Address:Port**|对端地址|
|**Process**|占用端口的进程|

---

## ### 🟥 netstat -anp（旧但面试常问）

```
netstat -anp
```

|参数|意思|
|---|---|
|**-a**|全部连接|
|**-n**|不解析域名|
|**-p**|显示进程|

---

## ### 🟦 ip addr（查看网卡）

示例：

```
2: eth0: <BROADCAST,MULTICAST,UP,LOWER_UP>
    inet 192.168.1.10/24 brd 192.168.1.255 scope global eth0
```

|字段|意思|
|---|---|
|**inet**|IPv4 地址|
|**/24**|子网掩码|
|**brd**|广播地址|
|**UP**|网卡已启用|
|**LOWER_UP**|物理 link 正常|

---

## ### 🟥 ip route（查看路由）

```
default via 192.168.1.1 dev eth0
192.168.1.0/24 dev eth0 scope link src 192.168.1.10
```

|字段|含义|
|---|---|
|**default**|默认路由|
|**via**|下一跳网关|
|**dev eth0**|走的网卡|
|**src**|源 IP|
|**scope link**|直连路由|

---

## ### 🟦 curl（测试 HTTP）

```
curl http://example.com
curl -I http://example.com
```

常见状态码：

|码|含义|
|---|---|
|**200**|OK|
|**301/302**|重定向|
|**403**|禁止|
|**404**|未找到|
|**500**|服务端错误|

---

# ## Ⅷ. 系统监控（System Monitoring）

```
df -h        # 磁盘剩余空间
du -sh .     # 当前目录占用
free -h      # 内存占用
vmstat 1     # CPU/内存综合监控
iostat       # 磁盘 IO
```

---

# # 🎯 最后必背总结（面试快问快答）

**1. 查看端口占用：**

```
ss -tulpn
```

**2. 查看 80 端口是谁占用：**

```
ss -tulpn | grep 80
```

**3. 查看所有网络配置：**

```
ip addr
ip route
```

**4. 查看进程状态：**

```
ps aux
top
```

**5. 杀进程：**

```
kill -15 PID   # 正常
kill -9 PID    # 强制
```
