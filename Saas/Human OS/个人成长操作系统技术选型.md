# 📘 Personal RPG System（PRS）技术准备文档 v1.0

## 1. 项目目标与约束

### 1.1 产品目标（技术视角）

- 用 **Web App** 实现一个本地优先（local-first）的「人生 RPG 系统」。
    
- 用户可以：
    
    - 初始化「角色」与「技能树」
        
    - 每天写日记
        
    - 通过 OpenAI API 自动解析行为 → 映射到技能 → 加经验
        
    - 在雷达图/成长曲线中看到自己的成长趋势
        
- **所有个人数据（包含日记等敏感内容）仅存本地（浏览器）**，可导出为 JSON，支持用户自行同步到 GitHub。
    

### 1.2 技术约束 / 原则

- 不在服务端存储任何与个人成长数据相关的内容。
    
- 服务端仅负责：订阅 & License 校验（未来可再细化）。
    
- 前端直接调用 OpenAI API，API Key 由用户自行输入并保存在本地。
    
- 可部署在 Vercel，尽量 serverless、无重型后端。
    

---

## 2. 总体技术选型

### 2.1 前端框架

- **Next.js (App Router) + React 18**
    
    - 原因：
        
        - 生态成熟，长期维护成本低。
            
        - 支持 SSR / SSG（虽然 v1 核心是 CSR，但保留弹性）。
            
        - 和 Vercel 天然兼容，部署体验极佳。
            

### 2.2 语言

- **TypeScript**（强制）
    
    - 好处：
        
        - 提高长期可维护性。
            
        - 对复杂数据结构（技能树、日志、XP 计算）有类型保护。
            
    - 默认：`strict: true`
        

### 2.3 UI / 样式

- **Tailwind CSS**
    
    - 快速搭建布局与主题（适合游戏风）。
        
- **shadcn/ui**
    
    - 提供基础组件：Button、Card、Dialog、Tabs、Input、Form 等。
        
    - 风格现代、易自定义，和 Tailwind 自然结合。
        

### 2.4 动画

- **Framer Motion**
    
    - 做升级动画、XP 条过渡、弹出成就等。
        

### 2.5 图表 & 可视化

- **Apache ECharts**
    
    - 用于：
        
        - 雷达图（六边形属性图）
            
        - 折线图（技能成长曲线）
            
        - 柱状图 / 饼图（行为分布）
            

### 2.6 状态管理

- **Zustand**
    
    - 负责：
        
        - UI 状态（打开的面板、当前选中角色等）
            
        - 当前 session 内的全局状态
            
    - 不直接作为持久化存储，而是与 IndexedDB 同步（读 / 写）。
        

### 2.7 本地数据存储

- **IndexedDB + Dexie.js**
    
    - Dexie 管理：
        
        - `config`（全局设置）
            
        - `characters`（角色存档）
            
        - `skillTrees`
            
        - `dailyLogs`
            
        - `achievements`
            
    - JSON 导入/导出 → 在 Dexie 层面做序列化。
        

### 2.8 AI 调用

- **OpenAI Chat Completions / Responses API**
    
    - 使用模型：`gpt-5.1`（未来可配置）
        
    - 调用方式：前端直接 `fetch` / `axios`。
        
    - API Key：
        
        - 用户在设置页输入
            
        - 保存在 IndexedDB 或 LocalStorage（v1 先不做加密）
            

### 2.9 后端 / 服务端

- 平台：**Vercel Serverless Functions**
    
- 职责（v1 可以只做最小版或暂时空壳）：
    
    - Stripe Webhook（订阅事件监听）
        
    - License 校验接口（接收 license key 返回是否有效）
        
- 不存储：
    
    - 日记内容
        
    - 技能数据
        
    - 用户成长记录
        
    - 用户 API Key
        

### 2.10 支付 & 授权

- **Stripe**
    
    - Subscription + Webhook
        
    - 生成 License Key（简单 UUID + Stripe customer 绑定）
        
- 本地：
    
    - 用户在客户端输入 license key
        
    - 调用后端 `/api/license/verify` 校验
        

### 2.11 部署

- **Vercel**
    
    - Frontend（Next.js）
        
    - API Routes（Stripe webhook, license check）
        

---

## 3. 项目结构规划

建议项目结构（简化示例）：

```plaintext
/ (repo root)
  ├─ src/
  │   ├─ app/              # Next.js App Router 目录
  │   │   ├─ layout.tsx
  │   │   ├─ page.tsx      # 主页（仪表盘/入口）
  │   │   ├─ settings/
  │   │   │   └─ page.tsx  # 设置页（API key, license, 主题等）
  │   │   ├─ character/
  │   │   │   ├─ new/
  │   │   │   │   └─ page.tsx  # 新建角色向导
  │   │   │   └─ [id]/
  │   │   │       ├─ page.tsx  # 某角色仪表盘
  │   │   │       └─ logs/
  │   │   │           └─ page.tsx # 日记列表与编辑
  │   │   └─ api/
  │   │       ├─ stripe/webhook/route.ts  # Stripe webhook
  │   │       └─ license/verify/route.ts  # License 校验
  │   │
  │   ├─ components/
  │   │   ├─ layout/
  │   │   ├─ charts/
  │   │   ├─ common/
  │   │   └─ rpg/
  │   │
  │   ├─ lib/
  │   │   ├─ db/           # Dexie 实现
  │   │   │   ├─ dexieClient.ts
  │   │   │   └─ schema.ts
  │   │   ├─ ai/           # 调用 OpenAI 的封装
  │   │   │   ├─ client.ts
  │   │   │   └─ prompts.ts
  │   │   ├─ models/       # TS 类型 & Domain Model
  │   │   ├─ services/     # 业务逻辑（XP计算、技能映射等）
  │   │   ├─ utils/        # 工具函数
  │   │   └─ config/
  │   │       └─ constants.ts
  │   │
  │   ├─ store/
  │   │   ├─ uiStore.ts    # UI 状态（模态框、侧边栏、主题）
  │   │   └─ sessionStore.ts # 当前角色、当前选中日期等
  │   │
  │   └─ styles/
  │       └─ globals.css
  │
  ├─ public/
  ├─ scripts/              # 一些辅助脚本（导出、导入工具等）
  ├─ .eslintrc.cjs
  ├─ .prettierrc
  ├─ tsconfig.json
  ├─ package.json
  └─ README.md
```

---

## 4. 数据模型设计（简版）

### 4.1 Dexie 数据库结构

```ts
// schema.ts
export const DATABASE_NAME = "prs-db";
export const DATABASE_VERSION = 1;

export const dexieSchema = {
  config: "id", // 单例配置
  characters: "id, name, createdAt, updatedAt",
  skillTrees: "id, characterId",
  dailyLogs: "id, characterId, date",
  achievements: "id, characterId, unlockedAt"
};
```

### 4.2 核心 TypeScript 类型

```ts
// models/character.ts
export interface Character {
  id: string;
  name: string;
  roleTemplate: "creator" | "engineer" | "entrepreneur" | "hybrid";
  age?: number;
  occupation?: string;
  goals?: string[];
  createdAt: string;
  updatedAt: string;
}

// models/skill.ts
export interface SkillNode {
  id: string;
  name: string;
  parentId?: string | null;
  characterId: string;
  level: number;
  xp: number;
  xpToNextLevel: number;
  tags: string[];           // 用于 AI 映射
  keywords: string[];       // 用于 AI 提示词
  createdAt: string;
  updatedAt: string;
}

// models/dailyLog.ts
export interface ParsedAction {
  description: string;
  skillIds: string[];
  intensity: "low" | "medium" | "high";
  durationMinutes?: number;
}

export interface SkillDelta {
  skillId: string;
  xpGained: number;
}

export interface DailyLog {
  id: string;
  characterId: string;
  date: string;             // YYYY-MM-DD
  rawText: string;
  parsedActions: ParsedAction[];
  skillDeltas: SkillDelta[];
  createdAt: string;
  updatedAt: string;
}
```

---

## 5. AI 调用与 Prompt 约定

### 5.1 调用封装

```ts
// lib/ai/client.ts
export async function analyzeDailyLog(params: {
  apiKey: string;
  logText: string;
  skillDefinitions: SkillNode[];
}): Promise<{
  parsedActions: ParsedAction[];
  skillDeltas: SkillDelta[];
}> {
  // 这里封装调用 openai 的逻辑
}
```

### 5.2 Prompt 思路（简要）

- 输入：
    
    - 用户的日记文本
        
    - 当前角色的技能列表（名称、标签、关键词）
        
- 输出：
    
    - 行为拆解（parsedActions）
        
    - 每个技能的 XP 增量（skillDeltas）
        
- 限制模型输出为结构化 JSON，前端直接 parse。
    

---

## 6. 代码风格与命名规范

### 6.1 语言与风格工具

- **TypeScript**
    
- **ESLint**
    
    - 使用 `eslint-config-next` 作为基础配置
        
    - 开启 TypeScript 支持
        
- **Prettier**
    
    - 统一格式化风格（2 空格缩进 / 单引号 / 无分号或有分号都可但要统一）
        

可以在 `package.json` 中加上：

```json
{
  "scripts": {
    "lint": "next lint",
    "format": "prettier --write ."
  }
}
```

### 6.2 命名规范

- **文件 / 目录**
    
    - React 组件：`PascalCase`（`SkillRadarChart.tsx`）
        
    - Hooks：`useSomething.ts`
        
    - 工具函数：`xxxxUtils.ts` / `formatDate.ts` 这类
        
    - Zustand store：`xxxStore.ts`
        
    - Type / Interface 文件：归类在 `models/` 下
        
- **变量 / 函数**
    
    - 小驼峰（`camelCase`）：`currentCharacter`, `analyzeDailyLog`
        
    - 布尔变量命名：
        
        - `isLoading`, `hasLicense`, `isDialogOpen`, `shouldShowOnboarding`
            
- **类型 / 接口**
    
    - 大驼峰（`PascalCase`）：`Character`, `SkillNode`, `DailyLog`
        
- **React 组件**
    
    - 函数组件：`PascalCase`
        
    - 一个文件只导出一个默认组件（大组件）
        

### 6.3 目录内约定

- `components/` 下不直接写业务逻辑，只负责 UI 和交互。
    
- `lib/services/` 负责业务级逻辑，如：
    
    - XP 计算
        
    - 技能树更新
        
    - 日志创建/更新
        
- `lib/db/` 专注于 Dexie 读写逻辑。
    

---

## 7. 错误处理与 UX 约定

### 7.1 API Key 相关

- 用户没填 API Key：
    
    - 在日记分析时提示：“请先在设置中填入 OpenAI API Key。”
        
- API 调用失败：
    
    - 提示用户错误（超额、网络问题等）
        
    - 不删除原有日记内容
        
    - 允许用户稍后重新分析同一条日记
        

### 7.2 本地存储失败

- IndexedDB 不可用时：
    
    - 降级方案：LocalStorage（容量有限，提示给用户）
        
    - 提示用户升级浏览器或使用桌面版（未来）
        

### 7.3 数据导出 / 导入

- 导出：
    
    - 默认导出一个包含所有数据的 JSON 文件（带版本号）。
        
- 导入：
    
    - 校验版本号
        
    - 提供“覆盖 / 合并”选项（v1 可先只做覆盖）
        

---

## 8. 安全与隐私策略（v1 实现级）

- 不在服务端存储任何日记、技能、成长数据。
    
- 不在服务端存储用户 API Key。
    
- 在前端用 IndexedDB 或 LocalStorage 保存：
    
    - 用户 API Key
        
    - 角色数据
        
    - 日记数据
        
- 文档中明确说明：
    
    - 所有个人数据仅存于本地浏览器
        
    - 用户可随时导出 / 删除
        
- 未来版本：
    
    - 增加本地加密（AES + 用户密码）
        

---

## 9. Git / 工作流建议

- 分支策略：
    
    - `main`：稳定发布分支
        
    - `dev`：开发分支
        
    - 功能分支：`feature/xxx-personal-rpg-skill-tree` 等等
        
- 提交信息格式：
    
    - 英文 / 中文都可，但建议统一简单格式：
        
        - `feat: add daily log parser`
            
        - `fix: xp calculation overflow`
            
        - `refactor: extract skill service`
            
        - `chore: update deps`
            

---

## 10. Vercel 部署约定

- 使用 Vercel 直接连接 GitHub 仓库。
    
- 环境变量：
    
    - Stripe 密钥（仅用于后端 Webhook）
        
    - 其他后端用到的配置
        
- 前端不需要在构建时注入 OpenAI Key（由用户运行时填写）。
    
- 注意：
    
    - Vercel 的 Serverless 函数有执行时间限制
        
    - Stripe Webhook route 使用 Edge Function 或 Node runtime（根据需要）
        