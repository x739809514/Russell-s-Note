好的，我现在按你的要求分 **四大部分** 系统化输出：

1. **用户填写表格的 JSON Schema（正式可用 / 可直接给前端填表）**
    
2. **AI 分析这些表格的 Prompt（技能生成 + 初始属性生成）**
    
3. **Onboarding 的页面结构设计（Next.js 结构 + 每页内容 + 交互）**
    
4. **最终生成《用户体验流 Markdown 文档》（完整流程文档）**
    

所有内容都会保持一致的结构和风格，便于你之后做开发。

---

# ✅ **第一部分：用户填写表格的 JSON Schema**

这个 Schema 是 **最重要的基础**，  
因为它：

- 是你 onboarding 表单的蓝图
    
- 是 AI 解析用户初始资料的输入
    
- 是系统构建技能树、初始属性、主线任务的原始数据
    

下面是完整 Schema（可直接用于前端 + 后端 + Prompt）

---

## 🎯 **`UserProfileSchema`（用户信息基础）**

```json
{
  "type": "object",
  "properties": {
    "name": { "type": "string" },
    "gender": { "type": "string", "enum": ["male", "female", "other", "prefer_not_to_say"] },
    "age": { "type": "number" },
    "birthday": { "type": "string", "format": "date" },
    "location": { "type": "string" },
    "languages": {
      "type": "array",
      "items": { "type": "string" }
    }
  },
  "required": ["name"]
}
```

---

## 🎯 **`LifeBackgroundSchema`（背景与现状）**

```json
{
  "type": "object",
  "properties": {
    "currentOccupation": { "type": "string" },
    "currentProjects": {
      "type": "array",
      "items": { "type": "string" }
    },
    "currentTasks": {
      "type": "array",
      "items": { "type": "string" }
    },
    "interests": {
      "type": "array",
      "items": { "type": "string" }
    },
    "strengths": {
      "type": "array",
      "items": { "type": "string" }
    },
    "weaknesses": {
      "type": "array",
      "items": { "type": "string" }
    },
    "selfDescription": { "type": "string" }
  }
}
```

---

## 🎯 **`UserGoalsSchema`（目标体系）**

```json
{
  "type": "object",
  "properties": {
    "shortTermGoals": {
      "type": "array",
      "items": { "type": "string" }
    },
    "midTermGoals": {
      "type": "array",
      "items": { "type": "string" }
    },
    "longTermVision": { "type": "string" },
    "desiredSkills": {
      "type": "array",
      "items": { "type": "string" }
    }
  }
}
```

---

## 🎯 **`SkillSeedSchema`（一级技能种子输入）**

```json
{
  "type": "object",
  "properties": {
    "skillSeeds": {
      "type": "array",
      "items": { "type": "string" }
    }
  }
}
```

---

## 🎯 **完整 Onboarding Schema（整合）**

```json
{
  "type": "object",
  "properties": {
    "profile": { "$ref": "#/definitions/UserProfile" },
    "background": { "$ref": "#/definitions/LifeBackground" },
    "goals": { "$ref": "#/definitions/UserGoals" },
    "skillSeeds": {
      "type": "array",
      "items": { "type": "string" }
    }
  },
  "definitions": {
    "UserProfile": { ... },
    "LifeBackground": { ... },
    "UserGoals": { ... }
  }
}
```

（在项目中分文件存放即可）

---

# ✅ **第二部分：AI 分析用户表格的 Prompt 设计**

你需要**两个核心 Prompt**：

1. **技能生成（Skill Expansion）Prompt**
    
2. **初始属性生成（Attribute Construction）Prompt**
    

下面给出可直接投入生产使用的版本。

---

# 🌱 **Prompt 1：技能扩展生成（Skill Expansion）**

## **指令模板：**

```
You will act as a Skill Tree Architect for a Personal RPG System.

User will provide:
1. Basic identity information
2. Life background and current situation
3. Goals and long-term vision
4. Skill seeds (user-defined top-level skills)

Your task is to:
1. Expand each skill seed into 4–10 well-structured sub-skills (second-level skills)
2. Optionally generate third-level skills (if applicable)
3. Generate tags, keywords, and typical example actions for each skill
4. Adapt skill depth and relevance based on the user's background and goals
5. Output clean JSON only (no explanation)

JSON structure:
{
  "skills": [
    {
      "id": "string",
      "name": "string",
      "children": [
        {
          "id": "string",
          "name": "string",
          "children": [...],
          "tags": ["..."],
          "keywords": ["..."],
          "examples": ["..."]
        }
      ],
      "tags": ["..."],
      "keywords": ["..."],
      "examples": ["..."]
    }
  ]
}

Rules:
- Use accurate terminology based on the user's domain.
- Avoid creating generic or meaningless skills.
- Skills must be relevant to user goals.
- All output must be valid JSON.
```

---

# 🔮 **Prompt 2：初始属性生成（Attribute Initialization）**

属性维度：

- 技术（Tech）
    
- 创造力（Creativity）
    
- 产品力（Product Sense）
    
- 影响力（Influence）
    
- 心智力（Mindset）
    
- 行动力（Execution）
    

每项等级 1–10。

---

## **指令模板：**

```
You are an Attribute Analyzer for a Personal RPG System.

Given the following:
- User identity
- Life background
- Current occupation
- Skills and strengths
- User goals and long-term vision

Generate the user's initial attribute panel.

Attributes:
- tech: 1-10
- creativity: 1-10
- product: 1-10
- influence: 1-10
- mindset: 1-10
- execution: 1-10

Rules:
- Values must be integers.
- Base attributes must align with user's background.
- Potential-based attributes should reflect user's goals and vision.
- Output detailed reasoning (internal) and final result (JSON).

Final output (JSON only):
{
  "attributes": {
    "tech": 0,
    "creativity": 0,
    "product": 0,
    "influence": 0,
    "mindset": 0,
    "execution": 0
  }
}
```

---

# ✅ **第三部分：Onboarding 页面结构设计（Next.js 实现结构）**

下面是 **非常具体** 的页面结构，可直接用于路由开发。

```
app/
 └─ onboarding/
     ├─ layout.tsx          
     ├─ page.tsx            # Step chooser / router
     ├─ step1-profile/
     │    └─ page.tsx
     ├─ step2-background/
     │    └─ page.tsx
     ├─ step3-goals/
     │    └─ page.tsx
     ├─ step4-skills/
     │    └─ page.tsx
     ├─ step5-skill-tree-preview/
     │    └─ page.tsx
     ├─ step6-attributes/
     │    └─ page.tsx
     └─ step7-summary/
          └─ page.tsx
```

---

# 🧩 页面结构（详细内容 + 交互）

## **① Step 1：Profile（个人信息）**

字段：

- 姓名
    
- 性别
    
- 年龄
    
- 生日
    
- 位置
    
- 语言
    

按钮：Next →

---

## **② Step 2：Background（人生背景）**

字段：

- 当前职业
    
- 当前项目
    
- 兴趣爱好
    
- 擅长技能
    
- 自我描述
    
- 主观优点 / 缺点
    

按钮：Next →

---

## **③ Step 3：Goals（目标体系）**

字段：

- 短期目标
    
- 中期目标
    
- 长期愿景
    
- 想学习的技能
    

按钮：Next →

---

## **④ Step 4：Skill Seeds（一级技能输入）**

输入方式：

- 输入框 + 按 Enter
    
- 或 AI 推荐列表供选择
    

按钮：

- Generate Skill Tree →（调用 AI）
    

---

## **⑤ Step 5：Skill Tree Preview（编辑技能树）**

展示：

- 二级技能展开
    
- 用户可删除 / 重命名 / 增加输入
    

按钮：Next →

---

## **⑥ Step 6：Initial Attributes（初始属性）**

展示雷达图  
用户可微调属性（可选）

按钮：Next →

---

## **⑦ Step 7：Summary（最终确认页）**

显示：

- Profile
    
- Background
    
- Goals
    
- Skill Tree（折叠版）
    
- Initial Attributes
    

按钮：**Create Character → 进入 Dashboard**

---

# ✅ **第四部分：最终《用户体验流 Markdown 文档》**

下面是你要的完整 Markdown 文档。

---

# 📘 **Personal RPG System — 用户体验流（Onboarding Flow）**

## 🎉 **Step 0：欢迎页**

- 启动应用
    
- 显示简短介绍
    
- 按钮 → 开始创建角色
    

---

## 🔹 **Step 1：个人信息（Profile）**

用户填写：

- 姓名
    
- 年龄
    
- 性别
    
- 生日
    
- 所在地
    
- 语言
    

---

## 🔹 **Step 2：人生背景（Background）**

用户填写：

- 当前职业
    
- 当前正在做的主要事情
    
- 目前正在推进的项目
    
- 兴趣爱好
    
- 擅长技能
    
- 想学习的技能
    
- 自我性格描述
    
- 优点与弱点
    

---

## 🔹 **Step 3：人生目标（Goals）**

用户填写：

- 短期目标（1 年内）
    
- 中期目标（3 年内）
    
- 长期愿景（5–10 年）
    
- 人生规划
    
- 想成为怎样的人
    
- 想培养的能力
    

---

## 🔹 **Step 4：技能输入（Skill Seeds）**

用户输入一级技能（自由输入）  
例如：

- Unity
    
- 写作
    
- 产品设计
    
- AI 技术
    

按 Enter 添加技能。

点击 → **AI 构建技能体系**

---

## 🔹 **Step 5：技能树预览（Skill Tree Preview）**

AI 自动生成：

- 二级技能
    
- 可选三级技能
    
- 每个技能的标签 / 关键词 / 示例
    

用户可编辑。

---

## 🔹 **Step 6：初始属性（Attributes）**

AI 根据用户的背景与目标生成：

- 技术
    
- 创造力
    
- 产品力
    
- 影响力
    
- 心智力
    
- 行动力
    

用户可进行微调。

---

## 🔹 **Step 7：总结（Summary）**

展示：

- 个人信息
    
- 背景
    
- 目标
    
- 技能树
    
- 初始属性雷达图
    

按钮 → **创建角色**

进入 Dashboard。